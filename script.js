/* 
  LOGIC BLUEPRINT: Level 1 - The Grid Generator
  
  TASK: 
  1. Define the dimensions of your grid (e.g., 10 rows, 10 columns).
  2. Create a "State" object to hold cell data. 
     - Think: What should the key be? (e.g., "A1", "B2").
     - What should the value be? (An object with 'raw' and 'processed' properties?).
  3. Select the grid container from the DOM.
  4. Use a nested loop (rows inside columns or vice versa) to:
     - Create a cell element (maybe a <div> or <input>).
     - Assign it a unique ID based on its coordinates.
     - Append it to the container.
  
  HINT: 
  - How do you turn the number 0 into the letter 'A'? 
  - Look up 'String.fromCharCode()'.
*/

// Step 1: Initialize your state constant here...

// Step 2: Write a function to generate the grid...

// Step 3: Call the function when the page loads...
